# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
 - checkout: self
     - task: GitHubAction@0
  inputs:
    githubConnection: 'github.com_Karthikg1310'
    repositoryName: 'Karthikg1310/DBT_Project1'
    actionName: '.github/workflows'
